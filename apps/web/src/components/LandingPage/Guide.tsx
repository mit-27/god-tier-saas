import Subtitle from "../ui/Subtitle"
import Title from "../ui/Title"

const Guide = () => {
  return (
    <div className="mt-2">
        <Title className="lg:text-5xl">Add End-to-End Type Safe APIs</Title>
        <Subtitle>
        Define your APIs with ts-rest Router, implement them in your NestJS{" "} 
        controllers, and consume them in NextJS using type-safe TanStack queries.
        </Subtitle>

        <div>
          

        </div>
    </div>
  )
}

export default Guide